// //**************************** don't format this file ****************************
// //**************************** don't format this file ****************************
// //**************************** don't format this file ****************************
// //**************************** don't format this file ****************************
// //**************************** don't format this file ****************************
// namespace snippet {
//     //line templete
//     export const lineTemplete = (line: string) => `
// ${line}`

//     //tab templete
//     export const tabTemplete = (code: string) => `  ${code}`;

//     //lines templete
//     export const linesTemplete = (lines: string[]) => lines.reduce((total, current) => total + lineTemplete(current), "");




// //property templete
// export const propertyTemplete = (name: string, type: string, modifier: Modifier = Modifier.PUBLIC) => {
// return `${modifier} ${name}: ${type}; 
// `;
// }; 

// //method templete
// export const methodTemplete = (name: string, params: string, logic: string, returnType: string, modifier: Modifier = Modifier.PUBLIC) => {
// return `${modifier} ${name}(${params}): ${returnType} {
//     ${logic}
// }
// `;
// };

// //class templete
// export const classTemplete = (className: string, properties: string, methods: string) => {
// return `
// class ${className} {
//     ${properties}
//     ${methods}
// }`;
// };

// //namespace templete
// export const nsTemplete = (nsName: string, className: string, properties: string, methods: string) => {
// return `
// namespace ${nsName} {
//     export class ${className} {
//         ${properties}
//         ${methods}
//     }
// }`;
// };




// }

